disk "/" {
    format = "💾 %avail"
}

memory {
    memory_used_method = "memavailable"
    format = "🐏 %available"
}

tztime local {
    format = "🕰 %a %b %d %H:%M:%S"
}

order += "disk /"
order += "memory"
order += "tztime local"
